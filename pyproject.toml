[project]
name = "libraryreports"
version = "0.1.0"
description = "Reports based on Sierra data"
requires-python = ">=3.13"
dependencies = [
    "nicegui>=3.7.1",
    "psycopg[binary]>=3.3.2",
    "python-dotenv>=1.2.1",
]
